# Feuille de route mobile (Expo)

V1 envisagée (prompt maître) :

- Authentification partagée avec l’API.
- Liste et détail dossier autorisés.
- Rapports et documents avec garde-fous de permission.
- Notifications (push à brancher après backend notifications).

Pas de validations sensibles en V1 mobile : préparation uniquement avec les mêmes endpoints que le web (`/api/v1/…`).
