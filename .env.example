# Copier ce fichier vers .env (racine du projet), puis remplacer par vos vraies valeurs.
# Avec le preset Cloudflare en dev : utilisez plutôt .dev.vars (voir .dev.vars.example).

# --- Resend (recommandé : API HTTPS, adapté Vercel / Cloudflare) ---
# https://resend.com/api-keys — la clé commence par re_ et n’est PAS une suite de x.
RESEND_API_KEY=re_5ctHE71N_7jciY3Pnm43TmzjdzuJ4zx4W
RESEND_FROM_EMAIL=Amanah Fiducie <onboarding@resend.dev>
CONTACT_TO_EMAIL=amanahfiducie@gmail.com

# --- Alternative : SMTP (ex. Gmail + mot de passe d’application) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_USER=amanahfiducie@gmail.com
# SMTP_PASS=your_gmail_app_password

# Développement uniquement : valider le formulaire sans envoyer d’e-mail (voir logs serveur).
# SKIP_CONTACT_EMAIL=1
